import sgMail from '@sendgrid/mail'; sgMail.setApiKey(process.env.SENDGRID_API_KEY || ''); export const sendBookingConfirmation = async (bookingData: any) => { const msg = { to: bookingData.email, from: process.env.FROM_EMAIL || 'noreply@makutravel.com', subject: 'MAKU Travel - Booking Confirmation', html: '<h1>Booking Confirmed</h1><p>Your pet-friendly booking has been confirmed.</p>' }; try { await sgMail.send(msg); console.log('Email sent'); } catch (error) { console.error('Email failed:', error); } };
